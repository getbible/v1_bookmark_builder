{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/getbible/v1_bookmark_builder/schema/locale.schema.json",
  "title": "Translated topic names for one locale (data/locales/<locale>.json and v1/locales/<locale>.json)",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "schema_version",
    "locale",
    "topics"
  ],
  "properties": {
    "schema_version": {
      "const": 1
    },
    "locale": {
      "type": "string",
      "pattern": "^[a-z]{2,3}(?:-[a-z0-9]{2,8})*$",
      "maxLength": 16
    },
    "name": {
      "type": "string",
      "minLength": 1,
      "maxLength": 80
    },
    "topics": {
      "type": "object",
      "propertyNames": {
        "type": "string",
        "pattern": "^[a-z0-9]+(?:-[a-z0-9]+)*$",
        "maxLength": 80
      },
      "additionalProperties": {
        "type": "string",
        "minLength": 1,
        "maxLength": 120
      }
    }
  }
}
